# SpamEater Docker Configuration
# Copy this file to .env and configure

# ============ REQUIRED ============
# Your email domain (e.g., example.com)
# Emails will be received at *@example.com
EMAIL_DOMAIN=example.com

# ============ OPTIONAL ============
# Web interface domain (defaults to EMAIL_DOMAIN)
# Can be a subdomain like mail.example.com
WEB_DOMAIN=app.example.com

# Admin email for Let's Encrypt notifications
# Defaults to admin@EMAIL_DOMAIN
ADMIN_EMAIL=

# Security tokens (auto-generated if not provided)
# Only set these if you want specific values
DELETE_TOKEN_SECRET=
CSRF_SECRET=
ENCRYPTION_KEY=

# ============ DEVELOPMENT ============
# Disable SSL for local development (default: false)
# Set to true to run HTTP-only
DISABLE_SSL=false

# Disable automatic SSL certificate generation (default: true)
# Set to false if you want to manually configure SSL
AUTO_SSL=true
