# Secrets are generated and persisted automatically on first start.
# Leave these blank unless you deliberately want to supply your own,
# in which case each must be 32+ characters. A short value here would
# overwrite the generated key and make stored mail undecryptable.
DELETE_TOKEN_SECRET=
CSRF_SECRET=
ENCRYPTION_KEY=
NODE_ENV=production
